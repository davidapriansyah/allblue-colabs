<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #3d89cc, #66a3e0); /* Latar belakang gradient biru */
            height: 100vh; /* Tinggi penuh viewport */
            display: flex; /* Menggunakan flexbox untuk sentralisasi */
            justify-content: center; /* Menyusun item di tengah secara horizontal */
            align-items: center; /* Menyusun item di tengah secara vertikal */
        }
        .login-container {
            background-color: #ffffff; /* Latar belakang putih untuk form */
            padding: 30px; /* Padding di dalam form */
            border-radius: 10px; /* Sudut membulat */
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); /* Efek bayangan */
            width: 300px; /* Lebar form */
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h2 class="text-center mb-4">LOGIN</h2>
        <form action="/admin/logAdmin">
            <div class="mb-3">
                <label for="username" class="form-label">Username</label>
                <input type="text" class="form-control" name="username" id="username" required>
            </div>
            <div class="mb-3">
                <label for="password" class="form-label">Password</label>
                <input type="password" class="form-control" name="password" id="password" required>
            </div>
            <button type="submit" class="btn btn-primary w-100">Login</button>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
